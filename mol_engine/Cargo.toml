[package]
name = "mol_engine"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mol_engine"
path = "src/main.rs"

[dependencies]
mol_utils = { path = "../mol_utils" }
mol_topology = { path = "../mol_topology" }
rhai = "1.19"
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ndarray = { workspace = true }
